- name: conditions in yaml
  hosts: local
  connection: local
  vars: 
    number: 5
  tasks: 
    -name: check number is less than 10
    ansible.builtin.debug:
      msg: "number is {{ number }} is less than 10"
    when: number < 10

# - name: conditions
#   hosts: local
#   connection: local
#   vars:
#     number: 12
#   tasks:
#   - name: check number is less than 10 or not
#     ansible.builtin.debug:
#       msg: "Given number {{ number }} is less than 10"
#     when: number < 10