- name: gathering facts in ansible
  hosts: local
  connection: local
  become: yes
  tasks: 
  - name: install nginx if it is redhat
    ansible.builtin.dnf:
      name: nginx
      state: latest
    when: ansible_os_family == "RedHat"


